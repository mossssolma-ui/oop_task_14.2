# 🛒 Интернет-магазин: Базовая структура с ООП

Проект представляет собой базовую объектно-ориентированную структуру интернет-магазина, реализованную на Python. В рамках домашнего задания по курсу "Введение в ООП" созданы основные сущности для электронной коммерции: **продукты** и **категории**.

## 📋 Описание функционала

### Основные сущности

#### `Product` (Продукт)
Класс для представления отдельного товара в магазине с атрибутами:
- `name` — название продукта
- `description` — описание продукта  
- `price` — цена продукта (с поддержкой копеек)
- `quantity` — количество товара в наличии (в штуках)

#### `Category` (Категория)
Класс для группировки продуктов по категориям с атрибутами:
- `name` — название категории
- `description` — описание категории
- `products` — список объектов `Product`, относящихся к категории

#### Smartphone (Смартфон) — дочерний класс Product
Класс для товаров категории "Смартфоны" с расширенными атрибутами:
- `efficiency` — производительность устройства
- `model` — модель смартфона
- `memory` — объем памяти в ГБ
- `color` — цвет корпуса
Особенность: метод `__add__` позволяет складывать только объекты Smartphone между собой, возвращая суммарное количество на складе.
#### LawnGrass (Газонная трава) — дочерний класс Product
Класс для товаров категории "Газонная трава" с атрибутами для садоводства:
- `country` — страна-производитель
- `germination_period` — срок прорастания
- `color` — цвет травы
Особенность: метод __add__ позволяет складывать только объекты LawnGrass между собой, возвращая суммарное количество на складе.

### Статистика
Класс `Category` автоматически отслеживает глобальную статистику:
- **Количество категорий** — общее число созданных категорий
- **Количество товаров** — общее число всех продуктов во всех категориях

### Дополнительные возможности
- Поддержка загрузки данных из JSON-файла (`products.json`)
- Автоматическое обновление статистики при создании новых объектов

## 🏗️ Структура проекта
```
.
├── main.py # Основной файл с примерами использования
├── src/
│ ├── init.py
│ ├── product.py # Класс Product
│ ├── category.py # Класс Category
│ ├── lawngrass_products.py
│ ├── smartphone_products.py
│ └── utils.py # подтягиевает категории и продукты из json-файла
├── tests/
│ ├── init.py
│ ├── conftest.py # файл с фикстурами
│ ├── test_lawngrass_products.py
│ ├── test_smartphone_products.p
│ ├── product.py.py # Тесты для класса Product
│ └── category.py.py # Тесты для класса Category
├── data/
│ └── products.json # Пример данных для загрузки
├── .flake8 # Настройки для линтера flake8
├── .gitignore # файл с исключениями для git
├── poetry.lock # Зависимости проекта
├── pyproject.toml # Зависимости проекта
└── README.md
```

## 🚀 Быстрый старт

### Требования
- Python 3.10+
- Poetry (для управления зависимостями)

### Установка
1. Клонируйте репозиторий:
   ```bash
   git clone: git@github.com:mossssolma-ui/oop_task_4.git
2. Установите зависимости:
    ```bash
   poetry install

## 🧪 Тестирование
### Проект покрыт unit-тестами с использованием pytest:
```
Name                                Stmts   Miss  Cover
-------------------------------------------------------
src\__init__.py                         0      0   100%
src\base_products.py                    5      0   100%
src\categ_iterator.py                  14      0   100%
src\categories.py                      30      1    97%
src\lawngrass_products.py              11      0   100%
src\print_mixin.py                      5      0   100%
src\products.py                        44      0   100%
src\smartphone_products.py             12      1    92%
src\utils.py                           18      0   100%
tests\__init__.py                       0      0   100%
tests\conftest.py                      46      0   100%
tests\test_base_products.py             4      0   100%
tests\test_categ_iterator.py           13      0   100%
tests\test_categories.py               55      0   100%
tests\test_lawngrass_products.py       16      0   100%
tests\test_print_mixin.py              14      0   100%
tests\test_products.py                 57      0   100%
tests\test_smartphone_products.py      17      0   100%
tests\test_utils.py                    30      0   100%
-------------------------------------------------------
TOTAL                                 391      2    99%
```
## 🔧 Используемые технологии
+ Python 3.12 — основной язык программирования
+ pytest — фреймворк для тестирования
+ Poetry — управление зависимостями и виртуальным окружением
+ flake8 — проверка кода на соответствие PEP 8

## 📄 Примеры использования
### Создание продуктов и категорий
```
from src.product import Product
from src.category import Category

# Создание продуктов
product1 = Product("Samsung Galaxy S23 Ultra", "256GB, Серый цвет, 200MP камера", 180000.0, 5)
product2 = Product("Iphone 15", "512GB, Gray space", 210000.0, 8)
product3 = Product("Xiaomi Redmi Note 11", "1024GB, Синий", 31000.0, 14)


# Создание категории
category1 = Category(
        "Смартфоны",
        "Смартфоны, как средство не только коммуникации, но и получения дополнительных функций для удобства жизни",
        [product1, product2, product3],
    )
print(f"Количество категорий: {Category.category_count}")
print(f"Количество товаров: {Category.product_count}")
```

## 🧩 Возможности для расширения

Проект спроектирован с учётом принципов ООП и легко поддаётся расширению. Вот несколько направлений, в которых можно развивать функционал:

### 1. Поддержка новых типов продуктов
Благодаря абстрактному базовому классу `BaseProduct`, можно легко добавлять новые категории товаров (например, `TV`, `Laptop`, `Book`), просто наследуясь от `Product` и добавляя специфичные атрибуты.

### 2. Валидация данных
- Добавить валидацию входных данных (например, запрет отрицательной цены или нулевого количества).
- Внедрить Pydantic или кастомные валидаторы для загрузки из JSON.

### 3. Улучшенная работа с ценами
- Поддержка нескольких валют.
- История изменения цен (хранение предыдущих значений).
- Применение скидок и акций.

### 4. Поиск и фильтрация
- Методы для поиска товаров по названию, ценовому диапазону или характеристикам.
- Фильтрация товаров внутри категории (например, «только смартфоны с памятью ≥ 256 ГБ»).

### 5. Работа с изображениями и медиа
- Добавить атрибут `image_url` в `Product`.
- Интеграция с CDN или локальным хранилищем изображений.

### 6. Логирование и мониторинг
- Добавить логирование операций (создание товара, изменение цены).
- Отслеживание подозрительных действий (например, частое понижение цены).

### 7. Улучшение итератора
- Поддержка пагинации при переборе товаров в категории.
- Фильтрация при итерации (например, только в наличии).

### 8. Экспорт данных
- Методы для экспорта каталога в CSV, Excel или JSON.
- Генерация отчётов по категориям.

Все эти улучшения можно внедрять без нарушения существующей архитектуры благодаря гибкой иерархии классов.

### Загрузка из JSON (дополнительно)
```
from src.utils import load_data_from_json

categories = load_data_from_json("data/products.json")
```